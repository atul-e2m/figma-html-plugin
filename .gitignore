# dependencies / builds
node_modules/
plugin/code.js
dist/

# secrets — the CLI reads .env; the plugin never sees it
.env
.env.*
!.env.example

# local test corpus: exported design bundles, generated sites, zips, result sheets
v2 tests/
*-ir/
*-ir-*/
out/
.plan-cache/
*.zip
*.xlsx

# os / tooling
.DS_Store
__pycache__/
